import { z } from 'zod'

export const {{entity}}CreateSchema = z.object({
})

export const {{entity}}UpdateSchema = z.object({
})

export const {{entity}}FindByIdSchema = z.object({
    id: z.string()
})

